{
  "tools": [
    {
      "name": "route_to_agent",
      "description": "Routes a subtask to a specific agent for execution",
      "parameters": {
        "agent_name": {
          "type": "string",
          "enum": ["code-generator", "validator"],
          "description": "The target agent to execute the subtask"
        },
        "task_id": {
          "type": "string",
          "description": "Unique identifier for this subtask"
        },
        "task_description": {
          "type": "string",
          "description": "Clear description of what needs to be done"
        },
        "context": {
          "type": "object",
          "description": "Additional context and parameters for the agent"
        }
      },
      "required": ["agent_name", "task_id", "task_description"]
    },
    {
      "name": "check_agent_status",
      "description": "Checks the execution status of a subtask",
      "parameters": {
        "task_id": {
          "type": "string",
          "description": "The task ID to check status for"
        }
      },
      "required": ["task_id"]
    },
    {
      "name": "aggregate_results",
      "description": "Combines results from multiple agents into final output",
      "parameters": {
        "task_ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of task IDs whose results should be aggregated"
        }
      },
      "required": ["task_ids"]
    },
    {
      "name": "create_feedback_loop",
      "description": "Creates a feedback loop when validation fails",
      "parameters": {
        "failed_task_id": {
          "type": "string",
          "description": "The task that failed validation"
        },
        "feedback": {
          "type": "string",
          "description": "Specific feedback about what needs to be fixed"
        },
        "retry_agent": {
          "type": "string",
          "description": "Which agent should retry with the feedback"
        }
      },
      "required": ["failed_task_id", "feedback", "retry_agent"]
    }
  ]
}
